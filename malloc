#include<stdio.h>
#include<stdlib.h>

int main()
{
    int *ptr,i;
    ptr=(int*)malloc(10*sizeof(int));

    for(i=0;i<10;i++)
    {
        ptr[i] = (i+1)*10;
    }

    for(i=0;i<10;i++)
    {
        printf("%d ",ptr[i]);
    }
    
    free(ptr);
    return 0;
}

