#include<stdio.h>
#include<stdlib.h>

int main()
{
    int *ptr,i;
    ptr=(int*)malloc(3*sizeof(int));

    for(i=0;i<3;i++)
    {
        ptr[i] = (i+1)*10;
    }

    for(i=0;i<3;i++)
    {
        printf("%d ",ptr[i]);
    }

    free(ptr);
    return 0;
}

